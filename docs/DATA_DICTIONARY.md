# Candidate data dictionary

The public roster retains the source's eighteen columns in order. Identity is
`queue_id` and `exponent`; screen and shell columns preserve structural
evidence; schedule columns preserve scheduler information; external,
assignment and result columns preserve their respective snapshots. Blank
values remain blank. Published rows are immutable.
