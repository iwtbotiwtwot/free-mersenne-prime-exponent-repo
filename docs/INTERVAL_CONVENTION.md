# Interval convention

Blocks use the source convention `lower < p <= upper`. A canonical block has
width 1,000,000, adjacent blocks meet exactly, and machine names use twelve
zero-padded digits. The excluded lower endpoint is never a member of a block.
